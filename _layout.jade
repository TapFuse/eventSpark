doctype
html
  head
    meta(charset='utf-8')
    meta(name='author', content='TapFuse')
    title Mobile Apps for Events and Conferences by TapFuse
    link(rel="stylesheet" href="styles.css")

    meta(name="viewport" content="minimal-ui, width=device-width,  initial-scale=1, maximum-scale=1.0, minimum-scale=1, user-scalable=no")
    meta(name='description', content='Mobile app for events and conferences to reach everyone, everywhere, to enhance networking. More attendee connections and engagement. More Info. Great ROI and more sponsor revenue.')
    meta(name="HandheldFriendly" content="True")
    meta(name="MobileOptimized" content="320")
    meta(http-equiv="cleartype" content="on")
    meta(name="apple-mobile-web-app-capable" content="yes")
    meta(name="apple-mobile-web-app-status-bar-style" content="black")
    meta(name="apple-mobile-web-app-title" content="Event management software platform")

  body(data-spy="scroll", data-offset="65", target="#navbar")
    != yield

  script(type='text/javascript', src='//cdn.jsdelivr.net/vivus/0.2.1/vivus.min.js')
  script(type='text/javascript', src='https://ajax.googleapis.com/ajax/libs/jquery/2.1.4/jquery.min.js')
  script(type='text/javascript', src='js/libs.js')


  script.
    smoothScroll.init({
    speed: 1000,
    easing: 'easeInOutCubic',
    offset: 70,
    updateURL: true,
    callbackBefore: function ( toggle, anchor ) {},
    callbackAfter: function ( toggle, anchor ) {}
    });


    $(document).on('click','.navbar-collapse.in',function(e) {
    if( $(e.target).is('a') ) {
    $(this).collapse('hide');
    }
    });

  script.
    new Vivus('animation', {type: 'delayed', duration: 150} );


    //-  clears form on after submiting

  script.
    function resetForm() {
    var frm = $('#contact_form')[0];
    if (frm.checkValidity()) {
        frm.submit(); // Submit
        frm.reset();  // Reset
        //- show modal thank you
        //- window.setTimeout(function() {
        //-     //- hide modal thank you
        //-     }, 2000)
    }
    return false; // Prevent page refresh
    };

